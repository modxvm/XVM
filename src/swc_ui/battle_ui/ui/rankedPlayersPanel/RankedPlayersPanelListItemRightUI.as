package
{
    CLIENT::WG {
        import net.wg.gui.battle.ranked.stats.components.playersPanel.list.PlayersPanelListItem;
        public dynamic class RankedPlayersPanelListItemRightUI extends PlayersPanelListItem
        {
        }
    }
    CLIENT::LESTA {
        import net.wg.gui.battle.ranked.stats.components.playersPanel.list.RankedPlayersPanelListItem;
        public dynamic class RankedPlayersPanelListItemRightUI extends RankedPlayersPanelListItem
        {
        }
    }
}
