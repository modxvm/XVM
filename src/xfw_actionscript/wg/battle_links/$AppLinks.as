package
{

internal class $AppLinks
{

/**
 *  @private
 *  This class is used to link additional classes into wg.swc
 *  beyond those that are found by dependecy analysis starting
 *  from the classes specified in manifest.xml.
 */
import net.wg.gui.battle.random.views.BattlePage; BattlePage;
import net.wg.gui.battle.battleloading.BattleLoadingForm; BattleLoadingForm;
import net.wg.gui.battle.ranked.RankedBattlePage; RankedBattlePage;
import net.wg.gui.components.containers.Atlas; Atlas;
import net.wg.infrastructure.base.AbstractView; AbstractView;
import net.wg.infrastructure.managers.impl.AtlasManager; AtlasManager;

}

}
