/**
 * XVM
 * @author Maxim Schedriviy <max(at)modxvm.com>
 */
package xvm.tankcarousel_ui
{
    import com.xfw.*;
    import com.xvm.*;
    import flash.utils.*;
    import net.wg.gui.lobby.hangar.tcarousel.MultiselectionSlotRenderer;

    public dynamic class UI_MultiselectionSlotRenderer extends MultiselectionSlotRendererUI
    {
        public function UI_MultiselectionSlotRenderer()
        {
            Logger.add(getQualifiedClassName(this));
            super();
        }
    }
}
